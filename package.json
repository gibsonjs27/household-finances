{
  "name": "household-finances",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --target=es2022 --minify --legal-comments=eof --outfile=assets/app.js",
    "test": "node --test tests/*.test.js",
    "dev": "node scripts/serve.js"
  },
  "dependencies": { "@azure/msal-browser": "5.21.0" },
  "devDependencies": { "esbuild": "0.28.2" }
}
